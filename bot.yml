services:
  - type: web
    name: no-im-not-a-human-bot
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: 3000
